# PDF Çevirmen v2 — OCR + PWA + Arayüz Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Taranmış PDF'ler/kapaklar/figür yazıları OCR ile çevrilsin; site kurulabilir PWA olsun; arayüz sade-profesyonel yenilensin.

**Architecture:** Tesseract.js tarayıcıda çalışır ve yalnızca yeni bir "blok kaynağı"dır: OCR paragrafları mevcut Block tipine dönüştürülür, mevcut çeviri→maskeleme→yerleştirme hattı değişmeden kullanılır. PWA `vite-plugin-pwa` ile eklenir. Arayüz framework'süz kalır.

**Tech Stack:** Mevcut yığın + tesseract.js (Apache-2.0), vite-plugin-pwa (MIT), sharp (devDep, ikon/örnek üretimi).

**Spec:** `docs/superpowers/specs/2026-07-04-v2-ocr-pwa-design.md`

## Global Constraints

- OCR güven eşiği: confidence < **60** olan paragraflar elenir; `isTranslatable` süzgeci de uygulanır.
- Figür modunda OCR bloğu, herhangi bir metin-katmanı bloğuyla alanının > **%30**'u örtüşüyorsa elenir.
- Tek Tesseract worker'ı oluşturulur ve sayfalar arasında yeniden kullanılır; dil değişince yeniden başlatılır.
- OCR dili eşlemesi: en→eng, de→deu, fr→fra, es→spa, it→ita, pt→por, nl→nld, ru→rus, uk→ukr, pl→pol, ar→ara, fa→fas, zh-CN→chi_sim, ja→jpn, ko→kor, hi→hin, el→ell, sv→swe, tr→tur, **auto→eng**, bilinmeyen→eng.
- OCR hatası sayfayı asla düşürmez: sayfa v1 yoluyla kopyalanır, sonda uyarı raporlanır.
- PWA: `display: standalone`; 192/512 px + maskable ikon; jsDelivr ve tessdata.projectnaptha.com **CacheFirst**; translate.googleapis.com, lingva örnekleri ve generativelanguage.googleapis.com **asla önbelleğe alınmaz**.
- Arayüz: vurgu rengi **#2563eb**; `prefers-color-scheme` ile otomatik koyu tema; framework eklenmez; tüm yeni metinler `src/strings.ts`'te.
- Her görev sonunda commit: İngilizce `feat:/test:/chore:` + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Kabuk PowerShell 5.1 (`&&` yok, `;` kullan); yol boşluklu — tırnakla.

## Dosya Yapısı

```
src/ocr/ocr.ts        # getOcrLang, extractParagraphs, ocrParagraphsToBlocks (saf, TDD)
src/ocr/overlap.ts    # filterNonOverlapping (saf, TDD)
src/ocr/manager.ts    # OcrManager: tesseract.js sarmalayıcı (dinamik import, worker yeniden kullanımı)
src/pipeline.ts       # PageStage.ocr?, PipelineOpts{ocrFigures}, onOcrPage olayı (TDD)
src/main.ts           # kutu + stage.ocr bağlama + OCR durum/uyarıları + registerSW
src/strings.ts        # yeni metinler
vite.config.ts        # VitePWA eklentisi
public/icon.svg, public/icons/*.png, scripts/make-icons.mjs
scripts/make-scanned-sample.mjs → public/ornek-taranmis.pdf
index.html, styles.css  # yeniden yazım
tests/ocr.test.ts, tests/overlap.test.ts, tests/pipeline.test.ts (ek)
```

---

### Task 1: OCR saf çekirdeği — dil eşleme, paragraf çıkarma, blok dönüşümü

**Files:**
- Create: `src/ocr/ocr.ts`
- Test: `tests/ocr.test.ts`

**Interfaces:**
- Consumes: `Block` (src/types.ts), `isTranslatable` (src/translate/filter.ts)
- Produces:
```ts
interface TessBbox { x0: number; y0: number; x1: number; y1: number }
interface OcrParagraph { text: string; bbox: TessBbox; confidence: number; lineCount: number }
getOcrLang(sourceCode: string): string
extractParagraphs(data: unknown): OcrParagraph[]
ocrParagraphsToBlocks(paras: OcrParagraph[], scale: number): Block[]
```

- [ ] **Step 1: Başarısız testi yaz**

`tests/ocr.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { getOcrLang, extractParagraphs, ocrParagraphsToBlocks } from "../src/ocr/ocr";

describe("getOcrLang", () => {
  it("bilinen kodları eşler", () => {
    expect(getOcrLang("en")).toBe("eng");
    expect(getOcrLang("de")).toBe("deu");
    expect(getOcrLang("zh-CN")).toBe("chi_sim");
    expect(getOcrLang("tr")).toBe("tur");
  });
  it("auto ve bilinmeyen kodlar eng'e düşer", () => {
    expect(getOcrLang("auto")).toBe("eng");
    expect(getOcrLang("xx")).toBe("eng");
  });
});

describe("extractParagraphs", () => {
  const para = (text: string, conf = 90) => ({
    text, confidence: conf,
    bbox: { x0: 100, y0: 200, x1: 500, y1: 260 },
    lines: [{}, {}],
  });
  it("v4 biçimi: data.paragraphs okunur", () => {
    const out = extractParagraphs({ paragraphs: [para("Hello world")] });
    expect(out).toHaveLength(1);
    expect(out[0]).toMatchObject({ text: "Hello world", confidence: 90, lineCount: 2 });
  });
  it("v5+ biçimi: data.blocks[].paragraphs düzleştirilir", () => {
    const out = extractParagraphs({ blocks: [{ paragraphs: [para("A b")] }, { paragraphs: [para("C d")] }] });
    expect(out.map((p) => p.text)).toEqual(["A b", "C d"]);
  });
  it("bbox'sız/boş paragraflar atlanır, boşluklar sadeleşir", () => {
    const out = extractParagraphs({
      paragraphs: [{ text: "  çok\n boşluk  ", confidence: 80, bbox: { x0: 0, y0: 0, x1: 10, y1: 10 } }, { text: "yok" }],
    });
    expect(out).toHaveLength(1);
    expect(out[0].text).toBe("çok boşluk");
  });
});

describe("ocrParagraphsToBlocks", () => {
  const p = (text: string, conf: number, lineCount = 2) => ({
    text, confidence: conf, lineCount,
    bbox: { x0: 100, y0: 200, x1: 500, y1: 280 },
  });
  it("piksel bbox'ı ölçeğe bölerek PDF puntosuna çevirir", () => {
    const [b] = ocrParagraphsToBlocks([p("Hello there world", 90)], 2);
    expect(b).toMatchObject({ x: 50, y: 100, width: 200, height: 40, translatable: true });
    // fontSize ≈ (40 / 2 satır) * 0.75 = 15
    expect(b.fontSize).toBeCloseTo(15, 0);
  });
  it("düşük güvenli paragraf elenir", () => {
    expect(ocrParagraphsToBlocks([p("Hello there", 40)], 1)).toHaveLength(0);
  });
  it("çevrilemez metin (yalnız sayı) elenir", () => {
    expect(ocrParagraphsToBlocks([p("12345", 95)], 1)).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Kırmızıyı gör** — Run: `npx vitest run tests/ocr.test.ts` → FAIL (modül yok).

- [ ] **Step 3: Uygula**

`src/ocr/ocr.ts`:
```ts
import type { Block } from "../types";
import { isTranslatable } from "../translate/filter";

export interface TessBbox { x0: number; y0: number; x1: number; y1: number }

export interface OcrParagraph {
  text: string;
  bbox: TessBbox;
  confidence: number;
  lineCount: number;
}

const MIN_OCR_CONFIDENCE = 60;

const OCR_LANGS: Record<string, string> = {
  en: "eng", de: "deu", fr: "fra", es: "spa", it: "ita", pt: "por",
  nl: "nld", ru: "rus", uk: "ukr", pl: "pol", ar: "ara", fa: "fas",
  "zh-CN": "chi_sim", ja: "jpn", ko: "kor", hi: "hin", el: "ell",
  sv: "swe", tr: "tur",
};

export function getOcrLang(sourceCode: string): string {
  return OCR_LANGS[sourceCode] ?? "eng";
}

interface TessParagraph {
  text?: string;
  confidence?: number;
  bbox?: TessBbox;
  lines?: unknown[];
}

/** Tesseract v4 (data.paragraphs) ve v5+ (data.blocks[].paragraphs) çıktısını normalleştirir. */
export function extractParagraphs(data: unknown): OcrParagraph[] {
  const d = data as { paragraphs?: TessParagraph[]; blocks?: Array<{ paragraphs?: TessParagraph[] }> };
  const paras = d.paragraphs ?? (d.blocks ?? []).flatMap((b) => b.paragraphs ?? []);
  const out: OcrParagraph[] = [];
  for (const p of paras) {
    if (!p.bbox) continue;
    const text = (p.text ?? "").replace(/\s+/g, " ").trim();
    if (!text) continue;
    out.push({ text, bbox: p.bbox, confidence: p.confidence ?? 0, lineCount: p.lines?.length || 1 });
  }
  return out;
}

/** OCR paragraflarını (canvas pikseli) mevcut Block tipine (PDF puntosu) çevirir. */
export function ocrParagraphsToBlocks(paras: OcrParagraph[], scale: number): Block[] {
  const blocks: Block[] = [];
  for (const p of paras) {
    if (p.confidence < MIN_OCR_CONFIDENCE) continue;
    if (!isTranslatable(p.text)) continue;
    const x = p.bbox.x0 / scale;
    const y = p.bbox.y0 / scale;
    const width = (p.bbox.x1 - p.bbox.x0) / scale;
    const height = (p.bbox.y1 - p.bbox.y0) / scale;
    if (width <= 0 || height <= 0) continue;
    blocks.push({
      text: p.text,
      x, y, width, height,
      fontSize: Math.max(6, (height / Math.max(1, p.lineCount)) * 0.75),
      bold: false,
      translatable: true,
      translated: null,
      failed: false,
    });
  }
  return blocks;
}
```

- [ ] **Step 4: Yeşili gör** — Run: `npx vitest run tests/ocr.test.ts` → 8 passed.
- [ ] **Step 5: Commit** — `git add -A; git commit -m "feat: add OCR core (lang map, paragraph extraction, block conversion)"` (+Co-Authored-By satırı).

---

### Task 2: Örtüşme süzgeci

**Files:**
- Create: `src/ocr/overlap.ts`
- Test: `tests/overlap.test.ts`

**Interfaces:**
- Consumes: `Block` (types)
- Produces: `filterNonOverlapping(ocrBlocks: Block[], textBlocks: Block[], threshold?: number): Block[]`

- [ ] **Step 1: Başarısız testi yaz**

`tests/overlap.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { filterNonOverlapping } from "../src/ocr/overlap";
import type { Block } from "../src/types";

const block = (x: number, y: number, w: number, h: number): Block => ({
  text: "t", x, y, width: w, height: h, fontSize: 10,
  bold: false, translatable: true, translated: null, failed: false,
});

describe("filterNonOverlapping", () => {
  const textBlocks = [block(100, 100, 200, 50)];
  it("metin katmanıyla büyük örtüşen OCR bloğu elenir", () => {
    // OCR bloğu metin bloğunun tam üstünde → %100 örtüşme
    expect(filterNonOverlapping([block(110, 105, 180, 40)], textBlocks)).toHaveLength(0);
  });
  it("örtüşmeyen OCR bloğu kalır (figür yazısı)", () => {
    expect(filterNonOverlapping([block(400, 400, 100, 30)], textBlocks)).toHaveLength(1);
  });
  it("küçük kenar teması (<%30) elenmez", () => {
    // OCR: 100x100 alanın yalnızca ~%10'u metin bloğuna değiyor
    const ocr = block(280, 130, 100, 100); // kesişim: 20x20=400 / 10000 = %4
    expect(filterNonOverlapping([ocr], textBlocks)).toHaveLength(1);
  });
  it("sıfır alanlı blok elenir", () => {
    expect(filterNonOverlapping([block(0, 0, 0, 10)], textBlocks)).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Kırmızıyı gör** — Run: `npx vitest run tests/overlap.test.ts` → FAIL.

- [ ] **Step 3: Uygula**

`src/ocr/overlap.ts`:
```ts
import type { Block } from "../types";

/**
 * Figür modunda, metin katmanının zaten kapsadığı bölgelerdeki OCR bloklarını
 * eler (aynı metni iki kez çevirmemek için). Eşik: OCR bloğu alanının %30'u.
 */
export function filterNonOverlapping(
  ocrBlocks: Block[],
  textBlocks: Block[],
  threshold = 0.3
): Block[] {
  return ocrBlocks.filter((o) => {
    const area = o.width * o.height;
    if (area <= 0) return false;
    for (const t of textBlocks) {
      const ix = Math.min(o.x + o.width, t.x + t.width) - Math.max(o.x, t.x);
      const iy = Math.min(o.y + o.height, t.y + t.height) - Math.max(o.y, t.y);
      if (ix > 0 && iy > 0 && (ix * iy) / area > threshold) return false;
    }
    return true;
  });
}
```

- [ ] **Step 4: Yeşili gör** — Run: `npx vitest run tests/overlap.test.ts` → 4 passed.
- [ ] **Step 5: Commit** — `git add -A; git commit -m "feat: add OCR/text-layer overlap filter"` (+Co-Authored-By).

---

### Task 3: Pipeline OCR dallanması

**Files:**
- Modify: `src/pipeline.ts`
- Test: `tests/pipeline.test.ts` (ekleme)

**Interfaces:**
- Consumes: `filterNonOverlapping` (Task 2), mevcut pipeline yapıları
- Produces (mevcutlara ek):
```ts
interface PageStage { ...; ocr?(pageNum: number): Promise<Block[]> }
interface PipelineEvents { ...; onOcrPage?(pageNum: number): void }
interface PipelineOpts { ocrFigures?: boolean }
runPipeline(pageNumbers, stage, events?, signal?, opts?: PipelineOpts)
```
Anlambilim: taranmış sayfada `stage.ocr` varsa ve blok dönerse sayfa NORMAL çeviri yoluna girer (`scannedPages`'e eklenmez); OCR boş dönerse v1 gibi kopyalanır ve `scannedPages`'e eklenir. `ocrFigures` yalnızca metin katmanlı sayfalarda ek OCR yapar; örtüşenler elenir.

- [ ] **Step 1: Başarısız testleri ekle**

`tests/pipeline.test.ts` dosyasının sonuna (describe bloğu içine) ekle:
```ts
  it("taranmış sayfa OCR blok dönerse normal çeviri yoluna girer", async () => {
    const stage = fakeStage({ 1: [] }); // metin katmanı yok → scanned
    stage.ocr = async () => [{
      text: "Scanned title text", x: 50, y: 100, width: 300, height: 30,
      fontSize: 12, bold: false, translatable: true, translated: null, failed: false,
    }];
    const onOcrPage = vi.fn();
    const result = await runPipeline([1], stage, { onOcrPage });
    expect(onOcrPage).toHaveBeenCalledWith(1);
    expect(result.scannedPages).toEqual([]);
    expect(result.translatedPages).toBe(1);
    expect(stage.added[0]).toBe(1); // çevrilmiş 1 blok yerleşti
  });
  it("taranmış sayfada OCR boş dönerse v1 gibi kopyalanır", async () => {
    const stage = fakeStage({ 1: [] });
    stage.ocr = async () => [];
    const result = await runPipeline([1], stage);
    expect(result.scannedPages).toEqual([1]);
    expect(stage.added[0]).toBe(0);
  });
  it("figür modu: metin sayfasında örtüşmeyen OCR bloğu eklenir", async () => {
    const stage = fakeStage({ 1: threeItems });
    const seen: string[][] = [];
    const origTranslate = stage.translate;
    stage.translate = async (texts, sig) => { seen.push([...texts]); return origTranslate(texts, sig); };
    stage.ocr = async () => [{
      text: "Figure caption text", x: 400, y: 500, width: 150, height: 20,
      fontSize: 10, bold: false, translatable: true, translated: null, failed: false,
    }];
    const result = await runPipeline([1], stage, {}, undefined, { ocrFigures: true });
    expect(seen[0]).toContain("Figure caption text");
    expect(result.translatedPages).toBe(1);
  });
  it("figür modu kapalıyken metin sayfasında OCR çağrılmaz", async () => {
    const stage = fakeStage({ 1: threeItems });
    const ocr = vi.fn(async () => []);
    stage.ocr = ocr;
    await runPipeline([1], stage);
    expect(ocr).not.toHaveBeenCalled();
  });
```
Not: `fakeStage` dönüş tipine `ocr` atanabilmesi için tipini `PageStage & { added: number[]; ocr?: PageStage["ocr"] }` yap (fonksiyonun dönüş tip imzasında `ocr?` zaten PageStage'te olacak — yalnızca `stage.ocr = ...` ataması derlensin diye mevcut tip yeterli olur).

- [ ] **Step 2: Kırmızıyı gör** — Run: `npx vitest run tests/pipeline.test.ts` → yeni 4 test FAIL.

- [ ] **Step 3: Uygula**

`src/pipeline.ts` değişiklikleri:
```ts
import { filterNonOverlapping } from "./ocr/overlap";

export interface PageStage {
  extract(pageNum: number): Promise<PageText>;
  translate(texts: string[], signal?: AbortSignal): Promise<(string | null)[]>;
  renderMasked(pageNum: number, blocks: Block[]): Promise<{ jpeg: Uint8Array; widthPt: number; heightPt: number }>;
  addPage(jpeg: Uint8Array, widthPt: number, heightPt: number, blocks: Block[]): Promise<void>;
  /** Sayfayı görselden okur; paragrafları Block olarak döndürür. Yoksa OCR devre dışı. */
  ocr?(pageNum: number): Promise<Block[]>;
}

export interface PipelineEvents {
  onPageDone?(done: number, total: number): void;
  onScannedPage?(pageNum: number): void;
  onOcrPage?(pageNum: number): void;
}

export interface PipelineOpts {
  /** Metin katmanlı sayfalarda da figür/resim yazılarını OCR'la. */
  ocrFigures?: boolean;
}
```
`runPipeline` imzası: `(pageNumbers, stage, events = {}, signal?, opts: PipelineOpts = {})`.
Döngü gövdesinde blok üretimi şöyle değişir:
```ts
    const pageText = await stage.extract(pageNum);
    let blocks = groupIntoBlocks(groupIntoLines(pageText.items));
    let ocrProvided = false;
    if (pageText.scanned && stage.ocr) {
      events.onOcrPage?.(pageNum);
      const ocrBlocks = await stage.ocr(pageNum);
      if (ocrBlocks.length > 0) {
        blocks = ocrBlocks;
        ocrProvided = true;
      }
    } else if (!pageText.scanned && opts.ocrFigures && stage.ocr) {
      events.onOcrPage?.(pageNum);
      const ocrBlocks = await stage.ocr(pageNum);
      blocks = blocks.concat(filterNonOverlapping(ocrBlocks, blocks));
    }
    const translatableIdx = blocks
      .map((b, i) => (b.translatable ? i : -1))
      .filter((i) => i >= 0);

    if ((pageText.scanned && !ocrProvided) || translatableIdx.length === 0) {
      if (pageText.scanned) {
        result.scannedPages.push(pageNum);
        events.onScannedPage?.(pageNum);
      }
      // ... (mevcut kopyalama yolu aynen)
```
Geri kalanı (çeviri yolu, güvenlik ağı, olaylar) değişmez.

- [ ] **Step 4: Yeşili gör** — Run: `npm test` → tüm testler geçer (77 + 16 yeni ≈ 93).
- [ ] **Step 5: Commit** — `git add -A; git commit -m "feat: add OCR branch to pipeline (scanned pages + figure mode)"` (+Co-Authored-By).

---

### Task 4: OcrManager — tesseract.js sarmalayıcı

**Files:**
- Create: `src/ocr/manager.ts`
- Modify: `package.json` (tesseract.js bağımlılığı)

**Interfaces:**
- Consumes: `extractParagraphs`, `OcrParagraph` (Task 1)
- Produces: `class OcrManager { recognize(canvas: HTMLCanvasElement, lang: string): Promise<OcrParagraph[]>; dispose(): Promise<void> }`

Birim testi yok (ağır WASM entegrasyonu); Task 8'de tarayıcıda doğrulanır. Saf mantık zaten Task 1'de test edildi.

- [ ] **Step 1: Bağımlılığı kur**

Run: `npm install tesseract.js@^6` → Expected: hata yok. (v6 kurulamazsa `tesseract.js@^5` kullan; API aynı.)

- [ ] **Step 2: Uygula**

`src/ocr/manager.ts`:
```ts
import { extractParagraphs, type OcrParagraph } from "./ocr";

type TessWorker = {
  recognize(image: HTMLCanvasElement, opts: object, output: object): Promise<{ data: unknown }>;
  terminate(): Promise<unknown>;
};

/**
 * Tesseract.js sarmalayıcı: worker yalnızca ilk ihtiyaçta (dinamik import)
 * oluşturulur ve sayfalar arasında yeniden kullanılır; dil değişince
 * yeniden başlatılır. WASM + dil verisi CDN'den gelir (SW önbelleğe alır).
 */
export class OcrManager {
  private worker: TessWorker | null = null;
  private lang = "";

  async recognize(canvas: HTMLCanvasElement, lang: string): Promise<OcrParagraph[]> {
    if (this.worker && this.lang !== lang) {
      await this.worker.terminate();
      this.worker = null;
    }
    if (!this.worker) {
      const { createWorker } = await import("tesseract.js");
      this.worker = (await createWorker(lang)) as unknown as TessWorker;
      this.lang = lang;
    }
    const { data } = await this.worker.recognize(canvas, {}, { blocks: true, text: true });
    return extractParagraphs(data);
  }

  async dispose(): Promise<void> {
    await this.worker?.terminate();
    this.worker = null;
  }
}
```

- [ ] **Step 3: Derlemeyi doğrula** — Run: `npm run build` → hata yok. `npm test` → hepsi geçer.
- [ ] **Step 4: Commit** — `git add -A; git commit -m "feat: add Tesseract.js OCR manager with worker reuse"` (+Co-Authored-By).

---

### Task 5: main.ts bağlama — kutu, durum, uyarılar

**Files:**
- Modify: `src/main.ts`, `src/strings.ts`, `index.html` (yalnızca kutu ekle; tam yenileme Task 7'de)

**Interfaces:**
- Consumes: `OcrManager` (Task 4), `getOcrLang`, `ocrParagraphsToBlocks` (Task 1), `PipelineOpts` (Task 3)
- Produces: yok (giriş noktası)

- [ ] **Step 1: strings.ts'e ekle**

`STR` nesnesine (retry satırından sonra):
```ts
  ocrFigures: "Görsellerdeki metinleri de çevir (daha yavaş)",
  ocrReading: (n: number) => `Sayfa ${n} görselden okunuyor…`,
  ocrPreparing: "Görsel okuma motoru hazırlanıyor (ilk kullanımda ~12 MB iner)…",
  warnOcrFailed: (n: number) =>
    `${n} sayfada görsel okuma başarısız oldu; bu sayfalar olduğu gibi bırakıldı.`,
```
`errScannedAll` metnini şununla DEĞİŞTİR:
```ts
  errScannedAll:
    "Bu PDF'te okunabilir metin bulunamadı (görsel okuma da sonuç vermedi). " +
    "Sayfalar boş ya da çok düşük kaliteli olabilir.",
```

- [ ] **Step 2: index.html'e kutuyu ekle**

`compare-toggle` satırının hemen üstüne:
```html
        <label><input type="checkbox" id="ocr-toggle" /> <span id="ocr-label"></span></label>
```

- [ ] **Step 3: main.ts'i bağla**

İçe aktarmalara ekle:
```ts
import { OcrManager } from "./ocr/manager";
import { getOcrLang, ocrParagraphsToBlocks } from "./ocr/ocr";
```
Statik metin bağlama bölümüne: `$("ocr-label").textContent = STR.ocrFigures;`

`startTranslation` içinde (`const bigJob = ...` satırından sonra):
```ts
  const ocrFigures = $<HTMLInputElement>("ocr-toggle").checked;
  const ocrManager = new OcrManager();
  let ocrFailedPages = 0;
  let ocrEngineReady = false;
```
`stage` nesnesine `ocr` üyesi ekle (renderMasked'ten önce):
```ts
      ocr: async (n) => {
        if (!ocrEngineReady) {
          statusEl.textContent = STR.ocrPreparing;
        }
        const page = await doc.getPage(n);
        const viewport = page.getViewport({ scale: 1 });
        let scale = computeScale(viewport.width, viewport.height);
        if (bigJob) scale = Math.min(scale, 1.4);
        const canvas = await renderPageToCanvas(page, scale);
        try {
          const paras = await ocrManager.recognize(canvas, getOcrLang(source));
          ocrEngineReady = true;
          return ocrParagraphsToBlocks(paras, scale);
        } catch (e) {
          console.error("OCR hatası:", e);
          ocrFailedPages++;
          return [];
        } finally {
          canvas.width = 0; canvas.height = 0;
        }
      },
```
`runPipeline` çağrısını güncelle: olaylara `onOcrPage: (n) => { statusEl.textContent = STR.ocrReading(n); }` ekle ve beşinci argüman olarak `{ ocrFigures }` geçir.
Bitiş raporuna (warnFailedBlocks satırından sonra): `if (ocrFailedPages > 0) warn(STR.warnOcrFailed(ocrFailedPages));`
`finally` bloğuna: `void ocrManager.dispose();`

- [ ] **Step 4: Doğrula** — Run: `npm test` → hepsi geçer; `npm run build` → hata yok.
- [ ] **Step 5: Commit** — `git add -A; git commit -m "feat: wire OCR into UI with figure-mode checkbox and status"` (+Co-Authored-By).

---

### Task 6: Taranmış örnek PDF + tarayıcı doğrulaması

**Files:**
- Create: `scripts/make-scanned-sample.mjs`, `public/ornek-taranmis.pdf`
- Modify: `package.json` (sharp devDep)

- [ ] **Step 1: sharp kur** — Run: `npm install -D sharp` → hata yok.

- [ ] **Step 2: Üretici script**

`scripts/make-scanned-sample.mjs`:
```js
// Taranmış PDF simülasyonu: metin SVG→PNG'ye rasterize edilir (metin katmanı YOK).
// Kullanım: node scripts/make-scanned-sample.mjs
import sharp from "sharp";
import { PDFDocument } from "pdf-lib";
import { writeFileSync } from "node:fs";

const svg = (title, lines) => `
<svg xmlns="http://www.w3.org/2000/svg" width="1190" height="1684">
  <rect width="1190" height="1684" fill="#fdfdf8"/>
  <text x="120" y="220" font-family="Arial" font-size="56" font-weight="bold" fill="#111">${title}</text>
  ${lines.map((l, i) => `<text x="120" y="${340 + i * 54}" font-family="Arial" font-size="34" fill="#222">${l}</text>`).join("")}
  <rect x="120" y="900" width="400" height="240" fill="#3b82f6"/>
  <text x="120" y="1200" font-family="Arial" font-size="28" fill="#333">Figure 1: A solid blue rectangle</text>
</svg>`;

const page1 = await sharp(Buffer.from(svg("The Scanned Chapter", [
  "This page was rasterized to an image, so it has",
  "no text layer at all. The application must use",
  "optical character recognition to read this text",
  "before it can translate it into another language.",
]))).png().toBuffer();

const page2 = await sharp(Buffer.from(svg("Second Scanned Page", [
  "Older books are often digitized as photographs.",
  "Each page is just a picture of the original paper.",
  "Version two of this application can now handle",
  "these documents thanks to the built-in OCR engine.",
]))).png().toBuffer();

const doc = await PDFDocument.create();
for (const png of [page1, page2]) {
  const img = await doc.embedPng(png);
  const page = doc.addPage([595, 842]);
  page.drawImage(img, { x: 0, y: 0, width: 595, height: 842 });
}
writeFileSync(new URL("../public/ornek-taranmis.pdf", import.meta.url), await doc.save());
console.log("public/ornek-taranmis.pdf yazildi");
```
Run: `node scripts/make-scanned-sample.mjs` → dosya oluşur (~200-400 KB).

- [ ] **Step 3: Tarayıcıda uçtan uca doğrula**

1. Dev sunucusunu başlat (preview aracı).
2. `/ornek-taranmis.pdf`'i programatik bırak, çeviriyi başlat.
3. Doğrula: durum "görselden okunuyor" gösterir → sayfalar çevrilmiş olarak önizlenir (başlık ve paragraflar Türkçe, mavi dikdörtgen korunmuş) → indirme çalışır → konsolda hata yok.
   **Not:** Google IP engeli sürüyorsa çeviri bekleme mesajına düşer; bu durumda OCR'ın metin bulduğunu durum akışından doğrula, tam doğrulamayı engel kalkınca tekrarla.
4. Normal `/ornek.pdf` ile de regresyon kontrolü yap (v1 akışı bozulmamış olmalı).

- [ ] **Step 4: Commit** — `git add -A; git commit -m "chore: add scanned sample generator and fixture"` (+Co-Authored-By).

---

### Task 7: PWA — ikonlar, manifest, service worker

**Files:**
- Create: `public/icon.svg`, `scripts/make-icons.mjs`, `public/icons/icon-192.png`, `public/icons/icon-512.png`, `public/icons/maskable-512.png`
- Modify: `vite.config.ts`, `src/main.ts` (registerSW), `tsconfig.json` (types), `package.json` (vite-plugin-pwa)

- [ ] **Step 1: Eklentiyi kur** — Run: `npm install -D vite-plugin-pwa` → hata yok.

- [ ] **Step 2: Logo ve ikonlar**

`public/icon.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="96" fill="#2563eb"/>
  <rect x="116" y="88" width="248" height="320" rx="20" fill="#ffffff"/>
  <rect x="156" y="150" width="168" height="18" rx="9" fill="#2563eb"/>
  <rect x="156" y="196" width="168" height="18" rx="9" fill="#93c5fd"/>
  <rect x="156" y="242" width="112" height="18" rx="9" fill="#93c5fd"/>
  <path d="M300 316 l84 56 -84 56 v-36 h-96 v-40 h96 z" fill="#1d4ed8"/>
</svg>
```

`scripts/make-icons.mjs`:
```js
import sharp from "sharp";
import { mkdirSync } from "node:fs";

mkdirSync(new URL("../public/icons", import.meta.url), { recursive: true });
const src = new URL("../public/icon.svg", import.meta.url).pathname.replace(/^\/(\w:)/, "$1");
for (const size of [192, 512]) {
  await sharp(src).resize(size, size).png().toFile(
    new URL(`../public/icons/icon-${size}.png`, import.meta.url).pathname.replace(/^\/(\w:)/, "$1"));
}
await sharp(src).resize(512, 512).png().toFile(
  new URL("../public/icons/maskable-512.png", import.meta.url).pathname.replace(/^\/(\w:)/, "$1"));
console.log("ikonlar üretildi");
```
Run: `node scripts/make-icons.mjs` → 3 PNG oluşur.

- [ ] **Step 3: vite.config.ts**

```ts
import { defineConfig } from "vite";
import { VitePWA } from "vite-plugin-pwa";

export default defineConfig({
  base: "./",
  build: { target: "es2022" },
  plugins: [
    VitePWA({
      registerType: "autoUpdate",
      includeAssets: ["fonts/NotoSans-Regular.ttf", "fonts/NotoSans-Bold.ttf", "icon.svg"],
      manifest: {
        name: "PDF Çevirmen",
        short_name: "PDFÇevir",
        description: "Ücretsiz, sınırsız, kurulumsuz PDF çevirisi — dosyanız bilgisayarınızdan çıkmaz.",
        lang: "tr",
        display: "standalone",
        theme_color: "#2563eb",
        background_color: "#f7f7fb",
        icons: [
          { src: "icons/icon-192.png", sizes: "192x192", type: "image/png" },
          { src: "icons/icon-512.png", sizes: "512x512", type: "image/png" },
          { src: "icons/maskable-512.png", sizes: "512x512", type: "image/png", purpose: "maskable" },
        ],
      },
      workbox: {
        globPatterns: ["**/*.{js,css,html,ttf,svg,png,webmanifest}"],
        maximumFileSizeToCacheInBytes: 4 * 1024 * 1024,
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/cdn\.jsdelivr\.net\/.*/i,
            handler: "CacheFirst",
            options: { cacheName: "tesseract-cdn", expiration: { maxEntries: 40 } },
          },
          {
            urlPattern: /^https:\/\/tessdata\.projectnaptha\.com\/.*/i,
            handler: "CacheFirst",
            options: { cacheName: "tessdata", expiration: { maxEntries: 12 } },
          },
          {
            urlPattern: /^https:\/\/(translate\.googleapis\.com|generativelanguage\.googleapis\.com|lingva\.ml|translate\.plausibility\.cloud)\/.*/i,
            handler: "NetworkOnly",
          },
        ],
      },
    }),
  ],
});
```

- [ ] **Step 4: registerSW + tipler**

`tsconfig.json` → `"types": ["vite/client", "vite-plugin-pwa/client"]`.
`src/main.ts` en üstüne (ilk import'lardan sonra):
```ts
import { registerSW } from "virtual:pwa-register";
registerSW({ immediate: true });
```

- [ ] **Step 5: Doğrula** — Run: `npm run build` → `dist/` içinde `sw.js` ve `manifest.webmanifest` oluşur; `npm test` geçer. Tarayıcıda: sayfa yüklenir, konsolda SW kayıt hatası yok.
- [ ] **Step 6: Commit** — `git add -A; git commit -m "feat: add installable PWA (manifest, icons, service worker)"` (+Co-Authored-By).

---

### Task 8: Arayüz yenileme

**Files:**
- Modify: `index.html` (tam yeniden yazım), `styles.css` (tam yeniden yazım), `src/strings.ts` (altbilgi metinleri)

- [ ] **Step 1: strings.ts'e altbilgi metinleri ekle**

```ts
  footerPrivacy: "Dosyanız bilgisayarınızdan çıkmaz",
  footerOpenSource: "Açık kaynak (MIT)",
  footerVersion: "v2",
```

- [ ] **Step 2: index.html tam sürüm**

```html
<!doctype html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#2563eb" />
  <title>PDF Çevirmen — Ücretsiz PDF Çevirisi</title>
  <link rel="icon" href="./icon.svg" type="image/svg+xml" />
  <link rel="stylesheet" href="./styles.css" />
</head>
<body>
  <header>
    <img src="./icon.svg" alt="" width="44" height="44" />
    <div>
      <h1 id="app-name"></h1>
      <p id="tagline"></p>
    </div>
  </header>

  <main>
    <section id="setup-screen" class="card">
      <div id="dropzone" tabindex="0" role="button">
        <svg viewBox="0 0 24 24" width="40" height="40" aria-hidden="true"><path fill="currentColor" d="M19 13v6H5v-6H3v8h18v-8zM11 3v10.17l-3.59-3.58L6 11l6 6 6-6-1.41-1.41L13 13.17V3z" transform="rotate(180 12 12)"/></svg>
        <p id="drop-hint"></p>
        <input type="file" id="file-input" accept="application/pdf,.pdf" hidden />
      </div>
      <div class="controls">
        <label><span id="source-label"></span><select id="source-lang"></select></label>
        <label><span id="target-label"></span><select id="target-lang"></select></label>
        <span id="engine-badge" class="badge"></span>
      </div>
      <details id="settings">
        <summary id="settings-title"></summary>
        <label>
          <span id="gemini-key-label"></span>
          <input type="password" id="gemini-key" autocomplete="off" />
        </label>
        <p id="gemini-key-hint" class="hint"></p>
      </details>
    </section>

    <section id="work-screen" class="card" hidden>
      <div class="controls">
        <label>
          <span id="range-label"></span>
          <input type="number" id="range-from" min="1" class="num" />
          –
          <input type="number" id="range-to" min="1" class="num" />
        </label>
        <label><input type="checkbox" id="ocr-toggle" /> <span id="ocr-label"></span></label>
        <label><input type="checkbox" id="compare-toggle" /> <span id="compare-label"></span></label>
      </div>
      <div class="controls">
        <button id="start-btn"></button>
        <button id="cancel-btn" class="secondary" hidden></button>
        <button id="download-btn" hidden></button>
      </div>
      <p id="status" role="status"></p>
      <progress id="progress" max="100" value="0" hidden></progress>
      <div id="warnings"></div>
      <div id="preview"></div>
    </section>
  </main>

  <footer>
    <span>🔒 <span id="footer-privacy"></span></span>
    <span>·</span>
    <span id="footer-oss"></span>
    <span>·</span>
    <span id="footer-version"></span>
  </footer>

  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```
`src/main.ts` statik bağlama bölümüne ekle:
```ts
$("footer-privacy").textContent = STR.footerPrivacy;
$("footer-oss").textContent = STR.footerOpenSource;
$("footer-version").textContent = STR.footerVersion;
```
(`privacy` paragrafı artık `gemini-key-hint` altında değil; `#privacy` elemanı kaldırıldıysa `$("privacy")` satırını da kaldır — bunun yerine gizlilik cümlesi dropzone altına `hint` olarak eklenebilir: `<p id="privacy" class="hint"></p>` öğesini `setup-screen` kartının sonunda TUT ve bağlamayı koru.)

- [ ] **Step 3: styles.css tam sürüm**

```css
:root {
  --accent: #2563eb;
  --accent-soft: #93c5fd;
  --fg: #17203a;
  --muted: #5b6478;
  --bg: #f4f6fb;
  --card: #ffffff;
  --line: #dfe4ef;
  --warn-bg: #fff7e0;
  --warn-line: #eab308;
  --err-bg: #feecec;
  --err-line: #dc2626;
}
@media (prefers-color-scheme: dark) {
  :root {
    --fg: #e7ebf5;
    --muted: #9aa3b8;
    --bg: #0f1524;
    --card: #1a2236;
    --line: #2a3450;
    --warn-bg: #33290e;
    --warn-line: #a17c08;
    --err-bg: #3a1414;
    --err-line: #b91c1c;
  }
}
* { box-sizing: border-box; }
body {
  font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
  color: var(--fg); background: var(--bg);
  margin: 0 auto; max-width: 960px; padding: 20px 16px 8px;
  display: flex; flex-direction: column; min-height: 100vh;
}
header { display: flex; align-items: center; gap: 14px; margin-bottom: 20px; }
header img { border-radius: 10px; }
header h1 { margin: 0; font-size: 1.5rem; letter-spacing: -0.02em; }
header p { margin: 2px 0 0; color: var(--muted); font-size: 0.9rem; }
main { flex: 1; }
.card {
  background: var(--card); border: 1px solid var(--line);
  border-radius: 16px; padding: 20px; margin-bottom: 16px;
}
#dropzone {
  border: 2px dashed var(--accent); border-radius: 12px;
  padding: 44px 16px; text-align: center; cursor: pointer;
  color: var(--accent); transition: background 0.15s;
}
#dropzone p { color: var(--fg); margin: 10px 0 0; }
#dropzone:hover, #dropzone.dragover, #dropzone:focus-visible { background: color-mix(in srgb, var(--accent) 8%, transparent); outline: none; }
.controls { display: flex; flex-wrap: wrap; gap: 12px 18px; align-items: center; margin: 16px 0 0; }
.controls label { display: flex; gap: 8px; align-items: center; font-size: 0.95rem; }
select, input, button {
  font: inherit; padding: 8px 12px; border-radius: 10px;
  border: 1px solid var(--line); background: var(--card); color: var(--fg);
}
input.num { width: 5em; }
button {
  background: var(--accent); color: #fff; border: none; cursor: pointer;
  font-weight: 600; padding: 10px 20px;
}
button:hover { filter: brightness(1.08); }
button:disabled { background: var(--muted); cursor: default; filter: none; }
button.secondary { background: transparent; color: var(--fg); border: 1px solid var(--line); }
.badge {
  background: color-mix(in srgb, var(--accent) 14%, transparent);
  color: var(--accent); border-radius: 999px; padding: 5px 14px; font-size: 0.85em; font-weight: 600;
}
.hint { color: var(--muted); font-size: 0.85em; }
details { border-top: 1px solid var(--line); margin-top: 16px; padding-top: 12px; }
summary { cursor: pointer; color: var(--muted); }
details label { display: flex; flex-direction: column; align-items: stretch; gap: 6px; margin-top: 10px; }
#status { min-height: 1.4em; font-weight: 500; }
progress { width: 100%; height: 10px; accent-color: var(--accent); }
#warnings p { background: var(--warn-bg); border: 1px solid var(--warn-line); color: #1a1a1a; border-radius: 10px; padding: 10px 14px; }
#warnings p.error { background: var(--err-bg); border-color: var(--err-line); }
#preview { display: flex; flex-direction: column; gap: 14px; margin-top: 16px; }
#preview .pair { display: flex; flex-direction: column; gap: 8px; }
#preview.compare .pair { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
#preview .pair canvas.original { display: none; }
#preview.compare .pair canvas.original { display: block; }
#preview canvas { width: 100%; height: auto; border: 1px solid var(--line); border-radius: 8px; background: #fff; }
footer {
  display: flex; gap: 10px; justify-content: center; align-items: center;
  color: var(--muted); font-size: 0.85rem; padding: 18px 0 10px;
}
```
Not: `#warnings p` kuralında `color: var(--fg)` kullan (koyu temada okunabilirlik).

- [ ] **Step 4: Tarayıcıda doğrula**

1. Dev sunucusu + sayfa aç; ekran görüntüsü al (açık tema).
2. `preview_resize` ile `colorScheme: "dark"` → koyu tema ekran görüntüsü; kontrast ve tüm metinlerin okunduğunu doğrula.
3. `/ornek.pdf` ile çeviri akışının çalıştığını doğrula (regresyon).
4. Konsolda hata yok.

- [ ] **Step 5: Doğrula ve commit** — `npm test`; `npm run build`; `git add -A; git commit -m "feat: redesign UI with clean light/dark theme and footer"` (+Co-Authored-By).

---

### Task 9: README güncelle + kapanış

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README'de "Sınırlar" bölümünü güncelle**

"Taranmış (görüntü) PDF'ler çevrilmez" maddesini şununla değiştir:
```markdown
- Taranmış (görüntü) PDF'ler dahili OCR (Tesseract) ile çevrilir; el yazısı ve çok
  düşük kaliteli taramalarda doğruluk sınırlıdır
```
"Özellikler" listesine ekle:
```markdown
- 🖨️ **Taranmış PDF desteği:** Görüntüden metin okuma (OCR) tarayıcıda çalışır; figür/kapak yazıları da isteğe bağlı çevrilir
- 📱 **Kurulabilir:** Tarayıcıdan "Uygulamayı yükle" ile masaüstü/telefon uygulaması gibi çalışır (PWA)
```

- [ ] **Step 2: Son doğrulama** — `npm test` (tümü geçer), `npm run build` (hata yok).
- [ ] **Step 3: Commit** — `git add -A; git commit -m "docs: update README for v2 (OCR, PWA)"` (+Co-Authored-By).

---

## Plan Öz-Denetimi (yazım sonrası kontrol edildi)

- **Spec kapsaması:** OCR çekirdeği (T1), örtüşme (T2), pipeline dalları (T3), Tesseract sarmalayıcı + CDN (T4), UI bağlama + metinler + errScannedAll güncellemesi (T5), taranmış örnek + e2e (T6), PWA/manifest/SW/önbellek kuralları/ikon (T7), arayüz yenileme + koyu tema (T8), README (T9). Spec'te "OCR blokları RawItem" denmişti; planda doğrudan `Block` üretiliyor (OCR çıktısı zaten paragraf — gruplama gereksiz); bilinçli sadeleştirme.
- **Tip tutarlılığı:** `OcrParagraph`/`TessBbox` T1'de tanımlı, T4 tüketiyor; `PageStage.ocr` T3 ile T5 birebir aynı imza; `PipelineOpts.ocrFigures` T3→T5.
- **Placeholder taraması:** T8'deki CSS koyu-tema bloğunda kasıtlı düzeltme notu var (bozuk satır + tam doğru blok); uygulayıcı doğru bloğu kullanmalı. Başka placeholder yok.
